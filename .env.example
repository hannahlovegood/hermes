# 复制本文件为 .env 后填写。没有 key 也能跑（自动演示模式，数据依旧真实）。

# Claude API Key —— 填上后即变成真·AI 深度分析
# 去 https://console.anthropic.com 申请，格式形如 sk-ant-...
ANTHROPIC_API_KEY=

# 可选：分析模型。默认 claude-opus-4-8（质量最好）。
# 想更便宜更快可改：claude-sonnet-4-6（约省一半）/ claude-haiku-4-5（最便宜）
HERMES_MODEL=claude-opus-4-8

# 可选：思考深度 low / medium / high。默认 medium（交互更跟手）；要更深可设 high
HERMES_EFFORT=medium

# ── 飞书机器人（可选，不填则 Web 单端运行）──────────────────
# 1) 飞书开放平台建「企业自建应用」，复制 App ID / App Secret 填到下面；
# 2) 开通权限 im:message（收发消息），事件订阅 im.message.receive_v1；
# 3) 事件回调地址填  http(s)://你的域名/api/feishu/webhook （加密留空）；
# 4) 本地调试可用 ngrok 等把 8000 端口暴露成公网地址。
# 填好后重启，在飞书里私聊机器人「贵州茅台」或「对比 茅台 和 五粮液」即可。
FEISHU_APP_ID=
FEISHU_APP_SECRET=
